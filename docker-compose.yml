version: '3.8'
services:
  discovery:
    build:
      context: ./discovery
      dockerfile: Dockerfile
    container_name: discovery_container
    ports:
      - "8761:8761"
    environment:
      - SPRING_APPLICATION_NAME=discovery-service
      - EUREKA_CLIENT_REGISTER_WITH_EUREKA=false
      - EUREKA_CLIENT_FETCH_REGISTRY=false
      - EUREKA_SERVER_ENABLE_SELF_PRESERVATION=false
    networks:
      - my_network
    volumes:
      - discovery-logs:/app/logs
    restart: unless-stopped

  gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
    container_name: gateway_container
    ports:
      - "8078:8078"
    depends_on:
      - discovery
      - chombre
    environment:
      - SPRING_APPLICATION_NAME=gateway-service
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery:8761/eureka/
      - SERVER_PORT=8078
      - SPRING_CLOUD_GATEWAY_DISCOVERY_LOCATOR_ENABLED=false
      - LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_CLOUD_GATEWAY=DEBUG
      - LOGGING_LEVEL_REACTOR_NETTY=DEBUG
    networks:
      - my_network
    volumes:
      - gateway-logs:/app/logs
    restart: unless-stopped

  chombre:
    build:
      context: ./chombre
      dockerfile: Dockerfile
    container_name: chombre_container
    hostname: chombre
    ports:
      - "8089:8089"
    depends_on:
      - discovery
    environment:
      - SPRING_APPLICATION_NAME=chombre-service
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery:8761/eureka/
      - SERVER_PORT=8089
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:chombredb
      - SPRING_DATASOURCE_DRIVERCLASSNAME=org.h2.Driver
      - SPRING_DATASOURCE_USERNAME=sa
      - SPRING_DATASOURCE_PASSWORD=
      - SPRING_H2_CONSOLE_ENABLED=true
      - SPRING_H2_CONSOLE_SETTINGS_WEB_ALLOW_OTHERS=true
    networks:
      - my_network
    volumes:
      - chombre-logs:/app/logs
    restart: unless-stopped

networks:
  my_network:
    driver: bridge

volumes:
  discovery-logs:
  gateway-logs:
  chombre-logs:
